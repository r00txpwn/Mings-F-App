# Route Chart

> Auto-generated by `scripts/generate-route-chart.js` — do not edit by hand.
> Last updated: **2026-05-17 06:43 UTC**

## Architecture Overview

| Surface | Entry Hostname / Path | Auth |
|---|---|---|
| Order App | `order.*` or `VITE_APP_SURFACE=order` | Public |
| Kiosk | any host `/kiosk` | Secret key |
| KDS | any host `/kds` | Secret key |
| Staff Panel | `sp.*` or default | Staff login |

## Visual Diagram

```mermaid
flowchart TD

  Browser(["Browser Request"])
  Browser --> SD{"Surface Detection\nmain.tsx"}

  SD -->|"order.hostname\nor VITE_APP_SURFACE=order"| OS["Order App"]
  SD -->|"path = /kiosk"| KK["Kiosk App"]
  SD -->|"path = /kds"| KS["KDS"]
  SD -->|"sp.hostname or default"| SP["Staff Panel"]

  subgraph OrderApp["Order App  (order.*)"]
    direction TB
    ORoot["/"]
    OF_browse["Browse\n(menu / cart / account)"]
    OF_checkout["Checkout"]
    OF_done["Order Placed"]
    ORoot --> OF_browse
    OF_browse -->|"Proceed to checkout"| OF_checkout
    OF_checkout -->|"Back"| OF_browse
    OF_checkout -->|"Order submitted"| OF_done
    OTrack["/track?token="]
    TrackView["Order Tracking"]
    OTrack --> TrackView
    OF_done -->|"Track link"| OTrack
  end

  OS --> OrderApp

  subgraph KioskApp["/kiosk"]
    direction TB
    KF_idle["Idle / Welcome"]
    KF_menu["Menu Browse"]
    KF_cart["Cart"]
    KF_checkout["Checkout"]
    KF_confirmation["Order Confirmed"]
    KF_idle --> KF_menu
    KF_menu --> KF_cart
    KF_cart --> KF_checkout
    KF_checkout --> KF_confirmation
    KF_confirmation -->|"Done / timeout"| KF_idle
    KF_menu -->|"60 s inactivity"| KF_idle
  end
  KK --> KioskApp

  subgraph KDSApp["/kds"]
    KDS_View["Kitchen Display\nReal-time order grid\npending → preparing → ready"]
  end
  KS --> KDSApp

  subgraph StaffPanel["Staff Panel  (sp.*)"]
    direction TB
    SAuth{"Auth Check"}
    SAuth -->|"not logged in"| SLogin["LoginScreen"]
    SAuth -->|"not staff"| SDenied["StaffAccessDeniedScreen"]
    SAuth -->|"staff ✓"| SNav["Nav"]

    SNav --> SS_home["/\nhome"]
    SNav --> SS_sales["/sales\nsales"]
    SNav --> SS_kiosk_orders["/kiosk-orders\nkiosk-orders"]
    SNav --> SS_menu_builder["/menu-builder\nmenu-builder"]
    SNav --> SS_combos["/combos\ncombos"]
    SNav --> SS_money["/money\nmoney"]
    SNav --> SS_reports["/reports\nreports"]
    SNav --> SS_products["/products\nproducts"]
    SNav --> SS_suppliers["/suppliers\nsuppliers"]
    SNav --> SS_expenses["/expenses\nexpenses"]
    SNav --> SS_payouts["/payouts\npayouts"]
    SNav --> SS_users["/users\nusers"]
    SNav --> SS_delivery["/delivery\ndelivery"]
    SNav --> SS_settings["/settings\nsettings"]
  end
  SP --> StaffPanel
```

## Staff Panel Routes

| Path | Screen |
|---|---|
| `/` | home |
| `/sales` | sales |
| `/kiosk-orders` | kiosk-orders |
| `/menu-builder` | menu-builder |
| `/combos` | combos |
| `/money` | money |
| `/reports` | reports |
| `/products` | products |
| `/suppliers` | suppliers |
| `/expenses` | expenses |
| `/payouts` | payouts |
| `/users` | users |
| `/delivery` | delivery |
| `/settings` | settings |

## Order App Flow

| State | Description |
|---|---|
| `browse` | Menu / cart / account tabs |
| `checkout` | Address, phone, fulfillment form |
| `done` | Confirmation + tracking link |

## Tracking

- Path: `/track?token={trackToken}`
- Public, no auth
- Real-time status updates via Supabase subscription

## Kiosk Flow

1. **idle** → 2. **menu** → 3. **cart** → 4. **checkout** → 5. **confirmation**

Inactivity timeout: 60 s → resets to **idle**

## Kitchen Display

- Single persistent view at `/kds`
- Real-time order grid: pending → preparing → ready
